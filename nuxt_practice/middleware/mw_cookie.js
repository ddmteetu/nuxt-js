export default function(context) {
  // if (process.server) { // to check the 'current environment', if it's server side or client side
  //   console.log(context.req.headers.cookie)
  // } else {
  //   console.log(document.cookie)
  // }
  // const all = context.app.$cookies.getAll()
  // console.log(all) // gives us 'object', cookies object
  // console.log(all.signed) // to access property value using their names
  // console.log(context.app.$cookies.get('signed')) // gives us the sigle property value, whose name is passed
}
